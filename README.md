# Simple Shell

Simple Shell is a C program that works as a simple nix shell.

## Usage
```bash
make simple_shell
./simple_shell
```

# built-in function to inspect /proc

```bash
$ proc 1/status
$ proc cpuinfo
```